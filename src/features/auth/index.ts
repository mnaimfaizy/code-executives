/**
 * Authentication & Authorization Feature Module
 * Entry point for the Auth learning module
 *
 * Covers modern authentication and authorization including:
 * - OAuth 2.0 and OpenID Connect protocols
 * - PKCE Authorization Code Flow
 * - Backend for Frontend (BFF) pattern
 * - Zero Trust Architecture
 * - WebAuthn / Passkeys
 * - RBAC, ABAC, and ReBAC authorization models
 * - AI Agent authentication via Token Exchange
 */

export { default } from './AuthPage';
export { default as AuthPage } from './AuthPage';

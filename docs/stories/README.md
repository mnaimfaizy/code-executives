# Story specs

One file per story visualization, `docs/stories/<module>/<story-id>.md`, written by the `viz-story-writer` agent from the template in `.agents/skills/step-model/spec-template.md`. A spec is approved by the owner before its step model is written; after that it records why each beat exists.
